server {
  listen 80;

  location / {
    root   /usr/share/nginx/html;
    index  index.html;
    try_files $uri $uri/ /index.html;
  }

  # Add this block to forward API requests to the backend
  location /api {
    proxy_pass http://backend:8080;
  }
}